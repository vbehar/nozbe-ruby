
RSpec

To run the specs, you need RSpec : 'sudo gem install rspec'

Then, just type 'spec spec/nozbe_* -f s'

Hopefully, everything should be ok...

See spec_helper.rb for the config (user account information)
